.main{
    num a = 5;
    call : test;
}

.test{
    str-in = "What is your name? :";
    syscall : write;
    syscall : read;
    
    str-in = "Welcome";
    syscall : write;
    str-in = str-out;
    syscall : write;
}